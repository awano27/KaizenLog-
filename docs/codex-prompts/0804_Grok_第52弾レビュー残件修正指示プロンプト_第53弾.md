# 第53弾: 第52弾レビュー残件修正・最終仕上げ4件（実装: Grok）

## 依存と前提

- **第51+52弾適用済みのワーキングツリーで作業すること**（HEAD `f01acb4` + 未コミット。コミットはしない）。
- テスト基準線: **`pytest 1111 passed`**。ここから減少させないこと。
- §R1（TZ変換）・§R3〜§R6 は合格済み。本弾は残る4件のみ。いずれも小修正であり、新機能・仕様変更は禁止。

## §S1 委譲小節前の空行が実出力に出ていない（第52弾§R8-1の再修正・❌残件）

`digest.py:316` の `out = ["", "### 🤝 …"]` は `digest.py:540` の `"\n".join(lines) + delegation` で連結されるため、先頭の `""` が直前行の改行1個にしかならず、実出力は `…摩擦の代理指標）\n### 🤝 委譲の形…` で**空行ゼロ**。

修正: `out` 先頭を `["", ""]` にするか、`digest.py:540` 側を `"\n" + delegation` にする（どちらか一方）。

- **受け入れ条件**: build_digest の**最終出力文字列**で委譲小節見出しの直前に空行1行があることを検証するテスト（部分文字列 `）\n\n### 🤝` 等の直接検証。レンダラ内部リストではなく連結後の文字列で見ること）。

## §S2 見出し「（14日）」のハードコード解消

`digest.py:316` の見出し `### 🤝 委譲の形（14日）` が固定のため、履歴5日の日は見出し「（14日）」+ 本文「（5日中央値 …）」と**同一小節内で矛盾**する。第52弾が潰した「嘘の14日ラベル」が見出しに残っている。

修正: 見出しから窓表記を削除し **`### 🤝 委譲の形`** とする（窓の情報は中央値行の「N日中央値」だけが持つ。見出しに実窓長を動的に入れる案は、中央値非表示日に窓が定義できないため採らない）。

- **受け入れ条件**: 履歴5日のテストで見出しに「14日」が含まれず、中央値行が「5日中央値」であること / 履歴14日でも見出しは「委譲の形」のみ。

## §S3 §D テスト強度の残り2点

`tests/test_round51_activation.py`:

1. **§D 区間外全文比較**: reflect の書き込みテストに、§A と同じ `_outside(before) == _outside(after)`（対象マーカー区間を除いた残り全文のバイト厳密比較）を追加する。現状 test_d2 は区間内比較のみ。
2. **脚注検証の無条件化**: `:537` 付近の `if fn_sec.strip():` ガード付きアサートを、**FOOTNOTES 区間に脚注定義が存在しなければ fail する**無条件アサートに変える。前提を満たすフィクスチャ（委譲小節の ※ 行が集約される状況、必要なら digest 内に既存 ※ を2本置いて max_inline=1 超過を確実化）を組んでから検証すること。

## §S4 test_b3 の検出力回復（第52弾で実質恒真化）

`tests/test_round51_activation.py:277` の `assert "14日中央値" not in sub.split("往復")[0]` は、§R2 のラベル実窓長化により履歴2日では「2日中央値」となるため何も検出しない。`"日中央値" not in` に修正し「最小3日未満は中央値非表示」を再び検証できるようにする。

## 検証コマンド（完了報告に出力を添付）

```
./.venv/Scripts/python.exe -m pytest -q
```

基準線 **1111 passed** から減少禁止。

実挙動確認（**一時ディレクトリのみ**・前弾と同じ手順）: 一時ボールトで `generate` を実行し、委譲小節の直前に空行があること・見出しが「### 🤝 委譲の形」であることの抜粋を報告に貼る。

## 禁止事項（厳守）

- git commit / push 禁止・実ボールト書き込み禁止・実 LLM 呼び出し禁止
- §S1〜§S4 以外のファイル・仕様に触らない
- 既存テストの削除・アサート弱体化禁止（§S3/§S4 は強化のみ）

## 完了報告様式

1. §S1〜§S4 の実装状況表（完了/未完了 + file:line）
2. `pytest -q` の結果（基準線 1111 から減少禁止）
3. 実挙動確認の抜粋（空行 + 見出し）
