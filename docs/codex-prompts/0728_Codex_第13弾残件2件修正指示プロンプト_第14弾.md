# Codex 修正指示: 第13弾レビュー残件2件 — 第14弾

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`(Python / pytest)

**前提: 第13弾適用済みのワーキングツリー(コミット 665d3ac 以降)。** 2件とも独立・小規模。

## §S1 [Medium] `ai_output_tokens` の入口ガード欠落と指標リスト未掲載

`src/kaizenlog/advice_format.py:371-380`、`src/kaizenlog/prompts/daily_advisor.md:86-88`、`src/kaizenlog/prompts/privacy_safe.md:72-73`、`src/kaizenlog/skills/daily-kaizen/SKILL.md:94-96`

**問題**: 新指標 `ai_output_tokens` が `_STRUCTURED_AI_PASS_METRICS` に未追加のため、構造化AIテレメトリ不在の環境でも PASS 条件として入口を通過し、判定側(`verdict.py:294` は skipped_unsupported 追加済み)との非対称で**永久未判定**になる — 第10弾 §M1c で塞いだ穴の再発。またプロンプト/スキルの使用可能指標リスト3ファイルに未掲載のため、LLM がこの指標を提案できない。

1. `_STRUCTURED_AI_PASS_METRICS` に `ai_output_tokens` を追加(入口ガードと判定側の対称性回復)
2. `daily_advisor.md` / `privacy_safe.md` / `daily-kaizen SKILL.md` の使用可能指標リストに `ai_output_tokens`(AI応答トークン量)を追記(各ファイルの既存の記載様式に合わせる)

テスト: 構造化AIテレメトリ不在環境で `ai_output_tokens` の PASS 条件が契約違反になる / 在る環境では通過する。

## §S2 [Low] `weekly-context --write` の CLI 経由テスト欠落

`tests/test_round13_w1_w2.py`

**問題**: 実装(`cli.py:1687-1714`)は成功/失敗とも log_run + 失敗時 stderr 警告で継続するが、既存テストは `log_run` を直接呼ぶ模擬のみで、CLI 実経路の検証が無い。

1. `main(["weekly-context", "--write", ...])` 経由の統合テストを2本追加:
   - 成功時: ノートに区間が書かれ、runlog に記録される
   - 書込失敗時(書込先を書込不能にする等のモック): stderr 警告が出て exit 0(無人実行を止めない)、runlog に失敗記録

テスト実行で確認: `python -m pytest -q` 全件通過。

---

## 受け入れ条件

- `python -m pytest -q` 全件通過(既存デグレなし)

作業完了時: 変更ファイル一覧、§S1〜S2 の対応状況、pytest 結果(要約)を報告すること。

## 禁止事項(毎回共通・厳守)

- **git commit / push / branch 操作を絶対にしない(第11〜13弾で3度違反。変更はワーキングツリーに残すだけ。コミットするかはユーザーが決める)**
- ssh / scp / リモートアクセスをしない
- DB スキーマ変更をしない
- テスト・実装から実 LLM・ネットワークを呼び出さない
- 外部ライブラリを追加しない(標準ライブラリのみ)
- タスクスケジューラへの登録をテストから実行しない
- バージョン番号の変更・CHANGELOG のリリース節の追加をしない
- マーカー区間外のノート内容・手書きテキストを変更するコードを書かない
