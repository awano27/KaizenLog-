# Codex 修正指示: 第17・18弾レビュー残件3件 — 第19弾

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`(Python / pytest)

**前提: 第17・18弾適用済みのワーキングツリー(コミット 7611198 以降)。** 3件とも独立・小規模。いずれも「テストが通っているのに実挙動が仕様と違う」タイプなので、修正はテスト先行(red→green)で行うこと。

## §F1 [Medium] FAIL側の機械構文違反が `json` に誤分類される

`src/kaizenlog/runlog.py:116-128`(classify_violation_kind)、`tests/test_round17_pass_machine.py`

**問題**: `json` 分類ルールの needle `"解析できません"` が、機械構文違反の文面「…機械構文として**解析できません**」に先にマッチするため、FAIL側(および文面によってはPASS側)の違反が `pass_not_machine_readable` でなく `json` に分類される(実行確認済み)。ヘルスレジャーでの機械構文違反の発生率観測(第17弾 §P1d の目的)が過小になる。

1. 分類の評価順を「特異的な needle が先」に変更するか、`pass_not_machine_readable` の needle を「機械構文として解析できません」のような特異的な文字列にする(両方でも可)
2. PASS側・FAIL側の両文面で `pass_not_machine_readable` に分類されるテストを追加(既存テストはPASS側のみ)

テスト: pass側/fail側の両文面の分類 / 真のJSON破損文面は引き続き `json`。

## §F2 [Medium] 摩擦スコアの「リトライ連鎖」項が常に0(デッドコード)

`src/kaizenlog/aiwork.py:659-688`(session_digests_for_stats)、`aiwork.py:703`(top_friction_sessions)、`src/kaizenlog/weekly_context.py:195`

**問題**: スコア式(エラー + 中断×5 + 連鎖×5)のうち、読み手(`d.get("retry_touch")`)が期待する `retry_touch` キーを `session_digests_for_stats` が一切書かないため、連鎖項が常に0。リトライ連鎖はこのツールの摩擦の主指標なのに、摩擦ワースト選定に効いていない。

1. `session_digests_for_stats` に `retry_touch` を書く: そのセッションの期間・プロジェクトがリトライ連鎖(detect時に得られる RetryChain のセッション/プロンプト対応)と重なる場合に連鎖数を設定。既存の RetryChain データから導出し、新規検出ロジックは追加しない
2. `retry_touch` 付きセッションがスコア上位に来ることを検証するテストを追加(既存テストは retry_touch を渡しておらず未検出だった)

テスト: 連鎖ありセッションのスコア加点 / 連鎖なしは従来どおり / 週次のワースト節に反映。

## §F3 [Low] Codex側 title のラッパー除去共通化

`src/kaizenlog/aiwork_codex.py:138-146`、`src/kaizenlog/aiwork.py:277`(_maybe_set_title)

**問題**: Claude側の title 抽出はコマンドラッパー除去(_is_command_wrapper)を通すが、Codex側は通さない。現状のCodexログ形式では実害は小さいが、抽出ロジックが2実装に分かれており将来の形式変更で挙動が割れる。

1. title 抽出(ラッパー除去→整形→40字切詰)を共通ヘルパーに切り出し、両アダプタから使う(挙動は現状のClaude側に合わせる)

テスト: 共通ヘルパーの単体テスト / 両アダプタの title が同一整形を通ることの確認。

---

## 受け入れ条件

- `python -m pytest -q` 全件通過(既存デグレなし)
- §F1/§F2 は「修正前に落ちるテスト」を先に書いてから修正すること(今回の2件はテストの穴が原因で検出漏れしたため)

作業完了時: 変更ファイル一覧、§F1〜F3 の対応状況、pytest 結果(要約)を報告すること。

## 禁止事項(毎回共通・厳守)

- **git commit / push / branch 操作を絶対にしない(変更はワーキングツリーに残すだけ。コミットするかはユーザーが決める)**
- ssh / scp / リモートアクセスをしない
- DB スキーマ変更をしない
- テスト・実装から実 LLM・ネットワークを呼び出さない
- 外部ライブラリを追加しない(標準ライブラリのみ)
- タスクスケジューラへの登録をテストから実行しない
- バージョン番号の変更・CHANGELOG のリリース節の追加をしない
- マーカー区間外のノート内容・手書きテキストを変更するコードを書かない
