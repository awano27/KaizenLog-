# Codex 修正指示: 自己計測の除外(advisor自身のLLM呼び出し混入)— 第25弾

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`(Python / pytest)

**前提: コミット 563b4fe 以降のワーキングツリー(pytest 557 passed が基準線)。**

## 背景(実物 2026-07-29 からの証拠)

advise/weekly が `claude -p` でLLMを呼ぶたびに `~/.claude/projects` にセッションログが残り、ClaudeCodeAdapter がそれを**ユーザーのAI作業として計測**している。7/29の実物では質テーブル5行中3行が「あなたは、観測事実から翌日の小さな実験を設計する生産性コーチです。…」= daily_advisor プロンプト自身。実害:

- セッション数・発話数・出力トークン(174kの大半)が自己呼び出しで水増し
- リトライ連鎖1回は**誤検知**(advisorの同型プロンプト連発を連鎖と判定)
- 「依頼の長さ別」の逆信号(「詳細な依頼・平均往復1.0」の正体がadvisorの長文プロンプト)

計測ツールが自分の計測行為を計測する自己参照は、すべての ai_* 指標とPASS判定の信頼性を壊す。**最優先で除外する。**

## §S1 [High] 内部呼び出しへのセンチネル付与

`src/kaizenlog/advisor.py`(claude-code-cli / copilot-cli バックエンドの呼び出し部)、weekly系で `claude -p` を使う箇所

1. KaizenLog が発行する全てのLLM呼び出しプロンプトの**先頭に1行のセンチネル**を付ける: `[kaizenlog-internal] 本行は計測除外用マーカーです。回答に影響させず無視してください。`
2. dry-run のプロンプト表示にも同一に含める(dry-run/本実行同一の既存原則を壊さない)
3. openai-compatible(Ollama)経路はセッションログを残さないため対象外(理由をコメントに明記)

## §S2 [High] アダプタ側の除外フィルタ

`src/kaizenlog/aiwork.py`(ClaudeCodeAdapter)、`src/kaizenlog/aiwork_codex.py`(該当すれば)

1. セッションの最初のユーザーメッセージが以下のいずれかに該当する場合、そのセッションを **internal として全指標から除外**(sessions / user_turns / tool_* / tokens / retry連鎖 / scan_user_prompts / 依頼長別 / title列):
   - センチネル `[kaizenlog-internal]` を含む(§S1 以降の呼び出し)
   - **同梱プロンプトテンプレートの冒頭と前方一致**(`prompts/*.md` 各ファイルの最初の非空行を正規化して比較。センチネル導入前の過去ログも遡って除外するため。テンプレ改訂に自動追従する設計にする — ハードコード文字列にしない)
2. 除外したセッション数は捨てずに集計し、質セクションに1行だけ表示: `内部呼び出し（KaizenLog自身のLLM実行）: N回を計測から除外`(N=0 の日は行ごと非表示)
3. stats v2 に `internal_ai_sessions` として保存(既存キーのセマンティクス変更はしない)

テスト: センチネル付きセッションの除外 / テンプレ冒頭一致の除外(過去ログ形) / 通常セッションは除外されない / 除外行の表示・非表示 / リトライ連鎖にinternalが混ざらない。

## §S3 [Low] 出力トークン行の重複表示解消

`src/kaizenlog/aiwork.py`(質サマリのレンダ部)

**問題**: コスト換算なし切替時、サマリ行末尾の「出力トークン: N」とフォールバック行「出力トークン: N（…コスト換算なし）」が**同じ数値を2回表示**する(実物7/29の61-62行目)。

1. フォールバック時はどちらか一方に統一する(数値の二重表示をなくす。例: フォールバック行を「推定コスト: -（モデル単価不明分が大半のため換算なし）」にしてサマリ行のトークン表示を維持)

テスト: フォールバック時に出力トークン数値が出力中に1回だけ現れる。

---

## 受け入れ条件

- `python -m pytest -q` 全件通過(既存デグレなし)
- §S2 のテンプレ前方一致がテンプレ文言の改訂に追従すること(テストでテンプレ先頭行を読み込んで検証)
- dry-run と本実行のプロンプト同一性テストが引き続き通る

作業完了時: 変更ファイル一覧、§S1〜S3 の対応状況、pytest 結果(要約)を報告すること。

## 禁止事項(毎回共通・厳守)

- **git commit / push / branch 操作を絶対にしない(変更はワーキングツリーに残すだけ)**
- ssh / scp / リモートアクセスをしない
- DB スキーマ変更をしない
- テスト・実装から実 LLM・ネットワークを呼び出さない
- 外部ライブラリを追加しない(標準ライブラリのみ)
- タスクスケジューラへの登録をテストから実行しない
- バージョン番号の変更・CHANGELOG のリリース節の追加をしない
- マーカー区間外のノート内容・手書きテキストを変更するコードを書かない
