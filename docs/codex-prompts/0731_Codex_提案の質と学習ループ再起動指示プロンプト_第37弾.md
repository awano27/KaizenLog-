# 0731 Codex 指示プロンプト 第37弾 — 提案の質と学習ループ再起動(T2-5〜T2-8)

あなたはこのリポジトリ(`C:\develop\KaizenLog\KaizenLog-`)の実装エージェントです。**第35弾(描画層一括改善)・第36弾(判定の2段階確定)が適用済みの状態**を前提に、提案の質と学習ループを扱う4件を実装する。Phase 1 → 5 の順に実装し、開始時に `./.venv/Scripts/python.exe -m pytest --collect-only -q -p no:cacheprovider` で基準線を再取得し、各Phase完了ごとに `./.venv/Scripts/python.exe -m pytest -q` で全PASSを確認すること（今回確認時点は `705 tests collected`）。**先行弾の変更と競合したら、先行弾の実装を正とし、本書の行番号ではなく関数名・定数名で再特定する。**

## 実行前ゲートと弾間契約

- 第35弾の `_metric_baselines_from_history`、中央値係数、`reader_notes` 空許容と、第36弾の `MemoryEntry.verdict_stage`、confirmed限定の消費側ガード、測定日再同期を現行コードとテストで確認する。1つでも欠けていれば実装を始めず、前提不足として報告する。
- 開始前に `git status --short`、HEAD、基準線テスト数、`metric_pass_rates` / `decay` / `render_actions_section` の現行シグネチャと全呼び出し箇所を確認する。行番号は補助情報であり、ずれたら関数名で読み替える。
- 実データの値・台帳状態は、実行環境で再確認できた場合だけ根拠にする。未確認なら合成fixtureで境界をテストし、実データ再現とは記載しない。ユーザーの既存ボールト更新は明示承認がある場合だけ許可する。
- **弾の依存:** 本弾は35/36の表示・stage契約を消費し、`MemoryEntry` の新フィールド追加や判定方式の変更は行わない。第38弾は本弾のconfirmed-only学習入力、PRM表示、因果仮説の保存形式を前提にする。

## 絶対的禁止事項(毎回同じ・違反厳禁)

1. **`git commit` / `git push` 禁止** 2. ssh/scp/リモート禁止 3. DBスキーマ変更禁止 4. 実LLM呼び出し禁止 5. マーカー外不可侵 6. 新規外部依存禁止 7. fail-closed(不明は「不明」、捏造禁止) 8. redact必須 9. **ユーザーのボールト(`C:/develop/obsidian/2026`)や台帳の既存データを書き換える一括migrationは行わない**(将来の生成分から正しくなればよい)

## 実データで確認された問題(根拠)

`C:/develop/obsidian/2026/Kaizen/Memory/` を実測:

- `suggestions.jsonl` 28行・ユニーク9件。status は **`proposed` 27 / `superseded` 1 で `done` は0件**。verdict は pass 7 / fail 2 / 未判定19。`metric_pass_rates`(memory.py:498)・`consecutive_fail_actions`(memory.py:587)・`detect_kzn_decay`(decay.py:198) の3機構が**すべて `status=="done"` を入口にしており、本番で一度も発火していない**
- KZN-20260727-001(`ai_tool_errors <= 500`・7/28に✅実測121)はその後 7/29=178 / **7/30=639(閾値超過)** / 7/31=48 と推移したが、日誌は「判定 ✅ 実測121」を毎日再掲し悪化がどこにも現れない
- `prompt_clusters.jsonl` 21件・**全件 `status="new"`**。上位は PRM-20260729-005 12回/1日(正規の依頼)、**PRM-20260729-007 12回/4日・-010 11回/6日・-009 10回/4日 は `<task-notification>` 系のシステム注入XML**、PRM-20260729-016「pushしてください」8回/4日、PRM-20260729-002 6回/3日。表示先は `weekly_context.py:309-320` の週次のみで、その週次ノートはボールトに1本も存在しない
- 7/30 は cs=210・稼働219.5分=**57.4回/時**、7/31 は cs=127・稼働64.6分=**118.0回/時**。生カウントと1時間あたりで順位が完全反転する。`experiments.py:31-54` にレート系指標が1つも無い
- `stats.py:154` が `tool_counts` を `most_common(5)` しか保存していない(7/30 の保存合計879 vs セッション表の実合計1084)

## Phase構成

| Phase | §ID | 内容 | 工数 |
|---|---|---|---|
| 1 | §A1-A4 | 稼働正規化PASS指標2件の追加(T2-6) | M |
| 2 | §B1-B3 | 学習ループの2トラック化(T2-8) | M |
| 3 | §C1-C2 | 因果仮説と反証条件の契約必須化・表示のみ(T2-7) | M |
| 4 | §D1 | 繰り返し依頼(PRM)の日次1行(T2-5) | S |
| 5 | §E1 | テスト + USAGE | S |

---

## Phase 1 — §A: 稼働正規化PASS指標(T2-6)

### §A1 指標定義の追加

- `experiments.py:31-54 METRIC_DESCRIPTIONS` に2件だけ追加する。`context_switches_per_hour`「1時間あたりのカテゴリ変更回数」/ `ai_tool_errors_per_session`「AI CLIセッション1回あたりのツールエラー回数」。**説明文にネスト括弧を入れない**(`metric_display_label`(experiments.py:59-73)が `[（(]` で切るため、括弧を入れると `strip_pass_annotation` が壊れる)
- `compute_metric`(experiments.py:127-187)に分岐追加。分母下限を**必ず**入れる: `context_switches_per_hour` は `summary.total_minutes < 60` なら `None`、`ai_tool_errors_per_session` は `len(cc_sessions) == 0` なら `None`。**0 で埋めない**(0埋めすると低稼働日が恒久偽PASSになる)。丸めは `round(v, 1)`
- `metric_from_stats`(experiments.py:190-300)に同じ2件を追加。`context_switches ÷ total_minutes × 60` と `ai.tool_errors ÷ ai.sessions`。分子分母のどちらかが欠落・分母が下限未満なら `None`
- **`ai_tool_error_rate`(÷ツール呼び出し)は実装禁止。** `stats.py:154` が `tool_counts` を `most_common(5)` しか保存しておらず分母が復元不能(7/30 は保存合計879 vs 実合計1084)。率を出すこと自体が「無い指標を有ると言う」側に倒れる
- 受け入れ条件: 稼働45分の合成summaryで `context_switches_per_hour` が `None`（0.0ではない）、稼働220分・cs210では `57.3`、稼働219.5分・cs210では `57.4` を返す。`ai_tool_errors=639`・AIセッション10件では `63.9`、セッション0件では `None` を返す。

### §A2 契約・可用性配線(ID衝突の予防線)

- `advisor.py:83-86 _BASIC_PASS_METRICS` に `context_switches_per_hour`、`advisor.py:87-95 _STRUCTURED_AI_PASS_METRICS` に `ai_tool_errors_per_session` を追加
- **`advice_format.py:453-466` に同名のローカル `frozenset` が重複定義されている。** 両方に足さないと入口ガードが片肺になる(HANDOFF「新機能のID/ラベルが既存と衝突」の再発)。**重複は残し、両方へ追加する**(§E1 が advisor 側の module-level tuple と advice_format 側の関数内ローカル frozenset の両方をアサートするため、lazy import で一本化するとローカル側が消えて §E1 のテストが書けなくなる)
- `available_pass_metrics`(advisor.py:104-121)経由で契約文(`{{KAIZENLOG_PASS_METRIC_CONTRACT}}`)に自動で載ることを確認する
- 受け入れ条件: `structured_ai_metrics_available=False` の evidence で `ai_tool_errors_per_session` を PASS に書いた JSON が契約エラーになる

### §A3 ベースラインとF10帯への登録(必須)

- `advice_evidence.py:220-234 _BASELINE_SIMPLE_METRICS` に2件を追加する。この定数を回すベースライン生成関数(HEAD では `_metric_baselines_from_stats` advice_evidence.py:237-261。**第35弾 §D2 適用後は `_metric_baselines_from_history(stats, history)` に置換され、値は当日実測ではなく履歴中央値になっている**)が `metric_from_stats` を回すため、定数への追加だけで値が入る。関数名は先行弾適用後の実装を正として読み替えること
- **登録しないと `_pass_challenge_error`(advice_format.py:59-61)の「ベースライン無しは検査しない」に落ち、新指標だけ挑戦性ガードが常時スキップになる。** 分母不足日は `None` が返るので自動的に検査スキップになり、これは正しい挙動
- F10帯(advice_evidence.py:790-856)にも追加する。履歴窓は実際に `build_advice_evidence` へ渡される設定済み窓を基準にし、14日を前提にするなら当日を含む過去14日以上をロードする配線を先に行う。`context_switches_per_hour` は cs と total_minutes が両方あり、測定可能日が3日以上のときだけ帯を出す。`ai_tool_errors_per_session` は既存 `ai_cc_sessions` 帯(advice_evidence.py:841-849)と同じく `structured_ai_metrics_available` を掛ける(入口ガードと対称でないとL2縮退する)。
- 受け入れ条件: 当日値と異なる履歴 `[55.0, 57.4, 60.0]` の中央値57.4をベースラインとして、当日を履歴へ混ぜずに `context_switches_per_hour <= 80` を契約エラー、`<= 54` を受理する。履歴2日以下、分母不足日、当日値だけでは帯を出さない。**係数は第35弾 §D2 の中央値係数を使い、1.2倍で計算しない。**

### §A4 分母不足日の「判定不成立」表示

- `judge_entries`(verdict.py:164-177)の `value is None → continue` は**変えない**(判定を捏造しないため)。問題は表示側で、`memory.py:1041-1053 _action_line` は verdict が無いと「7/30提案」としか出さず、分母不足で判定できなかった提案と未判定の提案を読者が区別できない
- `_action_line` に分岐を追加: verdict が無く、PASS指標が `_per_hour`/`_per_session` 系で、判定対象日の stats が存在し分母が下限未満のとき「(7/30提案・**判定不成立**・稼働45分のため分母不足)」と出す
- そのために `render_actions_section`(memory.py:971-975)に **`stats_history: Sequence[Mapping] | None = None` を追加**し、`cli.py:525`(`_write_actions_handoff`)/ `cli.py:710`(morning)/ `cli.py:1255`(done)の3呼び出しに `load_stats` を配線する。**加えて第36弾 §C3 が追加した「測定日ノート再同期」の `render_actions_section` 呼び出しも配線対象**(呼び出し箇所は grep で全数確認し、漏れが無いことを報告に書く)。**未指定なら現行出力そのまま**(後方互換)
- **達成断定の禁止**: 「判定不成立」に ❌ アイコンを使わない。失敗ではない
- 受け入れ条件: 稼働45分の翌日 stats を渡すと「判定不成立」が出て ❌ が出ない。`stats_history` 未指定の既存テストは1文字も変わらない

## Phase 2 — §B: 学習ループの2トラック化(T2-8)

### §B1 指標挙動トラックの新設

- **行動効果トラックの定義は一切変えない。** `metric_pass_rates`(memory.py:478-521)の `if e.status != "done"`(:498)と `_execution_aligned_verdict`(:503)、`consecutive_fail_actions`(memory.py:568-605)の `status == "done"`(:587)は**そのまま残す**
- `memory.py` に**新関数** `metric_behavior_rates(entries, today, *, window_days=30, min_judged=1)` を追加。`done` は要求しないが、対象は `status in ("proposed", "done")` かつ `verdict in ("pass","fail")` かつ `verdict_stage == "confirmed"` のエントリに限定する。`superseded/skipped/unmeasurable/graduated/retired` と provisional は除外する。戻り値は `metric_pass_rates` と同じ `(metric, passed, judged)` 形式
- 呼び出し先は `summarize_for_prompt`(memory.py:933 が `metric_pass_rates` を使っている箇所)に**並べて**出す。既存行を置き換えない
- **出力ラベルは必ず「実行の有無は問わない指標の挙動」。** 「達成」「改善した」「習慣化」と書かない(達成断定の禁止)
- 受け入れ条件: `status=proposed`・`verdict_stage=confirmed`・pass6/fail2の合成entriesで `metric_pass_rates` は空のまま、`metric_behavior_rates` が指標別PASS率を返す。provisional、superseded、skipped、unmeasurable、graduated、retiredを混ぜた場合は率・分母へ一切入らない。

### §B2 判定後の実測推移行

- `render_actions_section` に、`verdict_stage == "confirmed"` の pass/failエントリの直後に1行追加する。provisionalは確定後の実測推移の起点にしない:
  `  └ 判定後の実測: 7/29 178 ✅ → 7/30 639 ❌ → 7/31 48 ✅`
  `     (測定できた3日のうち1日が閾値超過。実行の有無は問わない指標の挙動です)`
- 評価は §A4 で渡す `stats_history` + `metric_from_stats` + `target_met`。範囲は `verdict_date` の翌日〜`target_day - 1`、**最大5日**
- 測定不能日はスキップし0で埋めない。全日測定不能なら**行ごと出さない**(情報ゼロ行を積まない)
- 受け入れ条件: 上記文字列を合成statsで再現できる。provisional起点では行が出ず、confirmed化後だけ出ること、ラベル文言がテストで固定されること、`stats_history` 未指定なら行が出ないことを確認する。

### §B3 decay の done ゲート除去

- `decay.py:198` の `e.status == "done"` を外す。`verdict == "pass"`(decay.py:199)・`verdict_date` 窓(decay.py:200-202)・`seen_ids` 重複防止(decay.py:208-212)・過半数条件(decay.py:232)は維持
- **第36弾 §D1 が同じ候補式(decay.py:194-203)に足した `e.verdict_stage == "confirmed"` は必ず残すこと。** done ゲートを外すのと同時に stage ゲートまで消すと、暫定判定が風化検出に流れ込む(第36弾の目的を無効化する)
- 発火有無の予測を受け入れ条件にしない。**本弾の成果指標は「§B1 の指標別PASS率が初めて算出可能になること」と、confirmed条件を保ったままdoneゲートだけを外すこと**に置く。実データで十分な測定日がない場合は `Unknown` とし、発火しないことをもって ❌ 判定にしない。
- 受け入れ条件: `status="proposed"` かつ `verdict="pass"` のエントリが、過半数違反する合成 stats で `DecayEvent` を返す

## Phase 3 — §C: 因果仮説と反証条件(T2-7・**表示のみ・第1段階**)

### §C1 契約への追加

- `src/kaizenlog/prompts/daily_advisor.md:66-74` の `actions` に `mechanism`(なぜ効くと考えるか)と `falsifier`(効かなかったと分かる条件)を追加し、`### フィールド規則`(:90-99)にも1行ずつ書く。**字数上限は50字**(40字だと例文が既に上限付近)・改行禁止
- `advice_format.py:401-` の actions ループ(`_validate_advice_raise` 内・ループは :510 まで続く)に検証を追加。既存の `_require_single_line` / `_check_no_kzn_or_marker` を流用する。**`_DIGIT_RE`(算用数字禁止)は `mechanism` にのみ適用**(`falsifier` は指標条件を書くため数字可)。`render_advice_markdown`から`render_reader_advice`へは内部専用の対応付けを渡し、最終Markdownでは非チェックボックスのインデント行として運ぶ。
- 必須化するが、欠落時は**既存の修復リトライ経路にそのまま乗せる**。新しい修復経路を作らない(ガードを増やしてLLMが緩い経路へ逃げる事故の予防)
- **第2段階として明示的にスコープ外**: `falsifier` を翌日の `judge_entries` で同時評価してはいけない。`MemoryEntry`(memory.py:41-51)は `action` 文字列しか持たず、`judge_entries` は `parse_pass_condition(entry.action)`(verdict.py:164)しか見ないため、永続化スキーマ追加なしには実装できない。**JSONL に falsifier を書かない**
- 受け入れ条件: mechanism/falsifier 欠落・51字・mechanism に算用数字で契約エラー。falsifier に数字を書いても通る。生成された `MemoryEntry` に falsifier が入っていない(第2段階の回帰固定)

### §C2 読者面への表示

- `render_reader_advice`(advisor.py:570-589)で、第35弾(T2-2)が復活させた `interpretation` / `next_metric` の行に続けて `- なぜ効くと考えるか: …` / `- 効かなかったと分かる条件: …` の2行を出す。JSON→`render_advice_markdown`→readerの全経路で対応付けを失わないこと。
- **機械可読の PASS/FAIL 構文を消さない。** `verdict.parse_pass_condition` の入力は memory の `action` 文字列であり、保存形式は変えない。F-ID 除去(advisor.py:576)は維持
- 素材が無い場合は行を出さない(第35弾が未適用の環境でも壊れないこと)
- **ID衝突の予防線(必須)**: mechanism/falsifier のサブ行は **素の箇条書き(`    - `)限定**にし、絶対に `- [ ]` 形式で書かない。`memory.py:22 _CHECKBOX_RE = ^(\s*- \[)([ xX\-])(\]\s*)(.*)$` は**インデント付きチェックボックスにもマッチする**ため `assign_action_ids`(memory.py:124)がサブ行にも KZN-ID を採番し `suggestions.jsonl` を汚染する。しかも `_assert_render_shape`(advice_format.py:552-556)は `^- \[ \] `(行頭固定)しか数えないので renderer 側では検出できない
- 受け入れ条件: 2行が出ても `parse_pass_condition` が同じ `(metric, op, target)` を返す / サブ行を含む出力を `assign_action_ids` に通し、**新規エントリ数がアクション数と一致**すること(サブ行にIDが振られないこと)

## Phase 4 — §D1 繰り返し依頼(PRM)の日次1行(T2-5)

- 配線先は `cli.py` の generate。`day_prompts` は `cli.py:222-226` で取得済み。`section` に aiwork を連結した直後(`cli.py:251-252`)、`store.write_section`(cli.py:272)の**前**に ACTIVITY 区間末尾の行として足す。**第35弾 §C2 の前日比変化テーブルも同じ区間に append される**ので、既存の append を消さず後ろに並べること(write_stats の `activity_sha256` は両方 append し終えた `section` から計算されること)。当日だけの`day_prompts`では累積日数を再構成できない。**既定は日次窓のままとし、表示行に「台帳の最終観測」と明記する**(`upsert_clusters` は promptledger.py:222-223 で count/days を max 更新するため、表示される「8回/4日」は当日観測ではなく台帳に残る過去の最大観測値。日次配線を変えずに済む)。固定ローリング窓の読み込みへ変える場合だけ、count/days の累積方法を別途定義する。
- 手順: `cluster_prompts(day_prompts)` → `count >= 2` で絞る → `upsert_clusters(cfg.memory_path, clusters, as_of=day, redactor=title_redactor)`(promptledger.py:165) → `load_prompt_ledger` で最新台帳を読み、表示は `status == "new"` かつ `count_total` 降順の上位2件
- **`aiwork._is_system_wrapper`(aiwork.py:223)を representative に必ず通し、True の行を除外する。** 実台帳では PRM-20260729-007/-010/-009(count 12/11/10)が `<task-notification>` 系のシステム注入XML。ただし**最上位の PRM-20260729-005(count 12)は正規の依頼文**なので、「上位N件を落とす」実装にせず**全件にフィルタを適用**すること
- 表示例(0件なら行ごと出さない):
  ```
  🔁 繰り返している依頼(台帳の最終観測): 「C:/develop/新規事業/gotouchi-ai-v2/PLAN.md この計画について…」12回/1日(PRM-20260729-005)、
     「pushしてください」8回/4日(PRM-20260729-016)
     → `kaizenlog prompts skill PRM-20260729-005` でスキル化できます(skilled にすると本行から消えます)
  ```
- 冪等性: `upsert_clusters` は count/days を max 更新する(promptledger.py:221-241)ため同日再実行で二重計上しない。日次窓(1日)は `kaizenlog prompts` の既定窓より狭いので max により台帳値は劣化しない
- **既存台帳の一括 migration はしない。** システム注入XMLの行は台帳に残したまま表示側で落とす
- 受け入れ条件: 4日分の合成プロンプトでcount/daysの意味が固定される / システム注入XML代表文が表示されない / `skilled` は出ない / 0件なら行が出ない / 同日2回generateしても`count_total`が増えない / 表示行に「台帳の最終観測」の明記が入っていること。

## Phase 5 — §E1 テスト + ドキュメント

- `tests/test_round37_proposal_quality.py` に §A1-§D1 の受け入れ条件を全て入れる。加えて回帰固定として:
  - `is_known_metric("ai_tool_error_rate")` が **False のまま**であること(実装禁止の固定)
  - 新指標2件が `_BASELINE_SIMPLE_METRICS` に**入っていること**を直接アサート(登録漏れ＝ガード常時スキップの固定)
  - advisor 側と advice_format 側の**両方**の `_STRUCTURED_AI_PASS_METRICS` に `ai_tool_errors_per_session` が入っていること、および構造化AIありの陽性fixtureで使用可能側へ出ること
- **テストが通るのに実挙動が違う欠陥の予防**: §A4・§B2 は `render_actions_section` を実際に呼び、出力文字列で検証する(定数の存在確認だけで済ませない)。§D1 は generate 相当の経路で `section` 文字列を組み立てて検証する
- `docs/USAGE.md`: 新PASS指標2件の意味と分母下限、「判定不成立」の読み方、「実行の有無は問わない指標の挙動」ラベルの意味を追記

## 仕上げ

1. `./.venv/Scripts/python.exe -m pytest -q` 全PASS（開始時に取得した基準線 + 本弾の追加分。固定値705を実行結果の代わりに使わない）
2. 完了報告は §ID 判定表(✅/⚠️/❌ + file:line + 追加テスト名)。未完は正直に。テスト未通過で ✅ 禁止

| §ID | 判定 | 根拠 (file:line) | 追加テスト |
|---|---|---|---|
| §A1 | | | |
| §A2 | | | |
| §A3 | | | |
| §A4 | | | |
| §B1 | | | |
| §B2 | | | |
| §B3 | | | |
| §C1 | | | |
| §C2 | | | |
| §D1 | | | |
| §E1 | | | |
