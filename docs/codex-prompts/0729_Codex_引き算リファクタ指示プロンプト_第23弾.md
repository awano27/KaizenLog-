# Codex 実装指示: 引き算リファクタ(二重契約の解消・死コード削除)— 第23弾

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`(Python / pytest)

**前提: 第22弾適用済みのワーキングツリー(pytest 553 passed が基準線)。** これは機能追加ではなく**削除の弾**。実装順は必ず §D1(置き換えの安全網を先に作る)→ §D2(削除)→ §D3(テスト移行)→ §D4(死コード)。

## 背景

LLM出力の検証はJSON契約層(advice_format.py)に一本化済みだが、旧Markdown契約 `advice_contract_errors`(advisor.py:453-549、97行)が**レンダラ出力の再検査**として残り、11検査のほぼ全てがJSON層と重複している(件数・PASS機械構文・KZN混入・evidenceゲート・意味検証)。二重契約は変更のたびに両方を撫でる保守コストであり、直近の弾でも「JSON層に足したガードをMarkdown層に足し忘れる」型のリスクが常にあった。関連ヘルパー込みで**約136行**、加えて呼ばれない関数2つ(**41行**)が削除できる。

**守るべきもの**: レンダラの回帰検知。`render_advice_markdown` の出力形状(見出し・チェックボックス・KZN行形式)には memory.py のID付与と verdict.py の判定注記が依存しており、形状が壊れたまま保存されると下流が静かに壊れる。よって「契約の重複」は消すが「形状の検査」は小さく残す。

## §D1 [先行] `_assert_render_shape()` を新設(置き換えの安全網)

`src/kaizenlog/advice_format.py`

1. レンダラ出力の**形状のみ**を検査する20行程度の関数を新設:
   - 読者向け3見出し(「今日の結論」「明日試すこと」「計測上の注意」)が存在する
   - チェックボックス行数 == actions 件数
   - `<!-- kaizenlog:` マーカー文字列・`[F` 系の根拠ID・KZN-ID(付与前段階のため)が混入していない
   - 違反時は現行と同じく `AdvisorError("renderer bug: ...")` を送出(呼び出し側の切り分け挙動を変えない)
2. 現在 `advice_contract_errors` を呼んでいるレンダ後検査(advice_format.py:595-609 付近)をこの関数の呼び出しに置き換える
3. **安全網の実証テストを先に書く**: render_advice_markdown をモックで壊し(見出し欠落・チェックボックス数不一致)、`AdvisorError` が引き続き送出されることを確認するテスト(red→greenの red がこのテスト)

## §D2 旧Markdown契約の削除

`src/kaizenlog/advisor.py`、`src/kaizenlog/advice_format.py`

1. 削除対象(§D1 が green になってから):
   - `advice_contract_errors`(advisor.py:453-549、97行)
   - `_numbered_items`(advisor.py:445-450)
   - `_is_measurable_condition`(advisor.py:645-647、528行からのみ被参照)
   - `collect_evidence_gated_errors`(advisor.py:607-621)と advice_format.py 側の triage 呼び出し(595-609 のうち §D1 で置き換えた残り)
2. 削除後、本番経路が「JSON契約検証(LLM出力)→ 決定論レンダ → `_assert_render_shape`(renderer bug検知)」の一直線であることを docstring かモジュールコメントで1箇所に明文化
3. **挙動不変の確認**: 修復ループ・L2縮退・dry-run同一プロンプトの各挙動に変更がないこと(既存テストが担保。触らない)

## §D3 テスト移行(49箇所/6ファイル)

`tests/test_advice_evidence.py`(28箇所)、`test_verdict.py`(6)、`test_round9.py`(6)、`test_advice_format.py`(5)、`test_round17_pass_machine.py`(2)、`test_ux_round8.py`(2)

1. `advice_contract_errors` を参照する各テストを3分類して処理:
   - (a) **JSON層に同等テストが既にある** → 削除(重複)
   - (b) **形状検査に相当** → `_assert_render_shape` 向けに書き換え
   - (c) **どちらにも同等が無い固有カバレッジ** → JSON層(validate_advice / _semantic 系)のテストとして移植
2. **固有カバレッジを失わないこと**が最重要。報告に「削除した/書き換えた/移植した」の3リストを必ず含める
3. 完了時の総テスト数が基準線(553)からどう変わったか、増減の内訳を報告する

## §D4 死コード削除

`src/kaizenlog/advisor.py`

1. `_observed_value_restatement_errors`(669-680、12行)と `_sanitize_repair_source`(846-874、29行)を削除(どちらも呼び出し箇所ゼロをgrepで確認してから)

---

## 受け入れ条件

- `python -m pytest -q` 全件通過
- §D1 の「壊れたレンダラで AdvisorError」テストが存在し、削除後も通る
- `grep -rn "advice_contract_errors\|_sanitize_repair_source\|_observed_value_restatement_errors\|collect_evidence_gated_errors" src/ tests/` がヒット0件
- advisor.py の行数削減を報告に含める(目安 -170行前後)

作業完了時: 変更ファイル一覧、§D1〜D4 の対応状況、テスト3分類リスト、pytest 結果(要約)を報告すること。

## 禁止事項(毎回共通・厳守)

- **git commit / push / branch 操作を絶対にしない(変更はワーキングツリーに残すだけ)**
- ssh / scp / リモートアクセスをしない
- DB スキーマ変更をしない
- テスト・実装から実 LLM・ネットワークを呼び出さない
- 外部ライブラリを追加しない(標準ライブラリのみ)
- タスクスケジューラへの登録をテストから実行しない
- バージョン番号の変更・CHANGELOG のリリース節の追加をしない
- マーカー区間外のノート内容・手書きテキストを変更するコードを書かない
- **削除の弾なので特に: 指示にない「ついでの改善」をしない(削除対象以外の関数・文面・テストを変更しない)**
