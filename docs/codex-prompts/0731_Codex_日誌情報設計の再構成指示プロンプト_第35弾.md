# 0731 Codex 指示プロンプト 第35弾 — 日誌情報設計の再構成(描画層9件)

あなたはこのリポジトリ(`C:\develop\KaizenLog\KaizenLog-`)の実装エージェントです。実日誌 2026-07-30 / 07-31 と実 stats 6日分を起点に確認された**情報設計の欠落**を、**描画層と、既存の `DailySummary.ai_tool_minutes` をstatsへ加算的に運ぶ配線だけ**で直す。Phase 1 → 5 の順に実装し、開始時に `./.venv/Scripts/python.exe -m pytest --collect-only -q -p no:cacheprovider` で基準線を再取得し、各Phase完了後と最終確認で全体テストを実行すること（今回確認時点は `705 tests collected`）。**`MemoryEntry` / `suggestions.jsonl` / verdict の永続化スキーマには一切触れない。新規計測ゼロ・LLM呼び出し増ゼロ**。statsへの新キーは既存値の保存だけに限定し、旧statsでキーが欠ける場合は注記を増やさない。起点データが実行環境に無い場合は再現したことにせず `Unknown` と報告する。

## 実行前ゲートと弾間契約

- 開始前に `git status --short`、現在のHEAD、対象シンボル、基準線テスト数を確認する。行番号は補助情報にとどめ、関数名・定数名・CLI名・テスト名で編集位置を再特定する。参照が見つからない場合は推測で代替せず停止して報告する。
- 実データ確認は既定で読み取り専用の合成fixtureまたは一時コピーを使う。`C:/develop/obsidian/2026` の既存ノート・台帳を更新する確認は、対象日・バックアップ・復元/差分確認をユーザーが明示承認した場合だけ行う。承認が無ければ実ボールト再生成を完了条件にしない。
- 第34弾の実装が現行HEADに存在することを関数・定数で確認してから開始する。本弾は描画と既存値の保存配線に限定し、既存の `reader_notes` 空値許容、PASS構文、KZN-ID、マーカー外バイト保全を壊さない。新しい計測イベント、外部取得、LLM呼び出しは追加しない。
- **弾の依存:** 本弾は先行弾なし。第36弾は本弾の描画結果を変更原因の切り分け対象として扱い、第37弾は本弾の reader-facing 表示と中央値ゲートを前提にし、第38弾は本弾で許容した空の `reader_notes` と表示区間の不変条件を前提にする。
- 完了判定はテスト結果だけで決めない。§IDごとに実装箇所、focused test、全体テスト結果、実挙動確認の有無を分けて報告する。未確認は `Unknown`、未実施は `⚠️` とし、`✅` にしない。

## 絶対的禁止事項(毎回同じ・違反厳禁)

1. **`git commit` / `git push` 禁止** 2. ssh/scp/リモート禁止 3. DBスキーマ変更禁止 4. 実LLM呼び出し禁止 5. マーカー外不可侵 6. 新規外部依存禁止 7. fail-closed(不明は「不明」、捏造禁止) 8. redact必須 9. **ユーザーのボールト(`C:/develop/obsidian/2026`)や台帳の既存データを書き換える一括migrationは行わない**(将来の生成分から正しくなればよい)

本弾固有の追加制約: (a) `MemoryEntry` のフィールド追加・`suggestions.jsonl` の書式変更・`verdict.parse_pass_condition` の入力仕様変更はいずれも禁止 (b) 新しく出す行に**達成断定**(「習慣化した」「改善した」)を書かない (c) 部分的にしか計測できていない軸で**割り算(カバレッジ%)を出さない** — 列挙にとどめる。

## 実データで確認された問題(根拠)

`.kaizenlog/stats/2026-07-30.json` と `2026-07-30.md` / `2026-07-31.md` を起点にした観測（実行時に対象fixtureの存在と値を再確認すること。再確認できない値は `Unknown`）:

- blocks 302件中 **3分未満が277件・計136.1分**(合計219.5分の62%)。タイムラインは「3分以上の画面ブロックを時刻順に表示。」の1行だけで、この62%と `コンテキストスイッチ: 210回` の行方を説明していない
- `ai.sources` = `codex{tool_errors:636, retry_chains:2}` / `claude-code{tool_errors:3, retry_chains:1}` を保存済みなのに、表示は `ツールエラー: 639回` の合算のみ(aiwork.py:1210-1214)
- `ai.session_digests` 10件・最大 `friction=454`。利用先は `weekly_context.py:191` だけで、**その週次ノートはボールトに1本も存在しない**
- `models=[claude-fable-5, claude-opus-5, gpt-5.4, gpt-5.6-sol, gpt-5.6-terra]` / `uncosted 431,521` / `output 432,110`(**99.9%が単価未登録**)なのに、日誌は「推定コスト: -」だけで何を設定すれば金額が出るのか読めない。ループ税の浪費 470,025 tok は当日出力の109%(第34弾の不変条件に関わる事実)だが桁区切りも比較対象も無い
- Kaizen 区間: 結論3文のうち2文が直上 Activity Log の逐語再掲、「明日試すこと」はチェックボックス1行のみ、「### 計測上の注意 / 現時点で、追加の計測上の注意はありません。」の**2行が死にテキスト**(7/27〜7/30 で4日連続)
- 画面AI作業は `by_app` で `ChatGPT.exe 71.7分` / `claude.exe 34.8分` だが 🧠 は CLI 10セッションのみ。それでも advice_evidence.py:885 `if not (ai_stats_valid and telemetry_sessions > 0):` により**CLIログが1件でもあれば注記が丸ごと抑止**され「注意はありません」と出る
- PASS閾値の挑戦性ベースラインは当日実測1点のみ(advice_evidence.py:237-261 の docstring 自身が「履歴中央値やF10帯は別経路」と明記)。`ai_tool_errors` は 101/770/121/178/639 とスパイクが激しく、7/30 なら `639×1.2=766` まで通る

## Phase構成

| Phase | §ID | 内容 | 工数 |
|---|---|---|---|
| 1 | §A1-A3 | 引き算と分離(死にテキスト除去・ソース別分離・カバレッジ宣言) | M |
| 2 | §B1-B2 | 🧠区間(摩擦ワースト1行・コスト/ループ税の意味化) | M |
| 3 | §C1-C2 | 📊区間(タイムライン説明率・前日比変化テーブル) | M |
| 4 | §D1-D2 | 🚀区間(LLM解釈の復活・PASS閾値ゲートの中央値化) | M |
| 5 | §E1 | テスト・USAGE・実挙動確認 | S |

---

## Phase 1 — §A: 引き算と分離

### §A1 情報ゼロ行の引き算(T1-1)

- `advice_evidence.py:907-908` の `if not reader_notes: reader_notes.append("現時点で、追加の計測上の注意はありません。")` を**削除**し、`reader_notes` が空タプルになり得るようにする。消費側は `advisor.py:580` の1箇所のみ(grep 済み)
- `advisor.py:580-589 render_reader_advice`: `evidence.reader_notes` が空なら **`### 計測上の注意` の見出しごと出力しない**。注意が実際に出る日(例 7/26 のAFK未計測)の表示は1文字も変えない
- `aiwork.py:785-804 retry_chain_excerpts`: 同一プロジェクト・同一正規化本文の連鎖起点を**畳んで `… ×N件`** と出す(7/30 は3行中2行が同一文字列)。並び順は最初の出現順を保持。`max_chains=3` は畳んだ**後**の行数に対して適用する
- 同関数: redact→`normalize_prompt_text`→30字切りの順序は現行のまま変えない(HANDOFF 記載の既知の切詰め順問題は本弾スコープ外)。本文が空になった場合のみ `連鎖起点（proj）: （依頼本文が無いため省略）` を出す
- 期待動作: 7/30 再生成で「リトライ連鎖起点」行が3行→2行以下、Kaizen 区間から「計測上の注意」見出しと定型文が消える
- 受け入れ条件: `reader_notes=()` の evidence で `render_reader_advice` の出力に `計測上の注意` が含まれないこと / `reader_notes=("AFK…",)` では含まれること / 同一文2件+別文1件を渡した `retry_chain_excerpts` が2行を返し1行目が `×2件` を含むこと

### §A2 ツールエラーのソース別分離(T1-3)

- `aiwork.py:1204-1214`: `ツールエラー: N回` / `ユーザー中断・拒否: N回` / `リトライ連鎖: N回` を、**当日のセッションが2ソース以上に分かれる日だけ** `639回（codex 636 / claude-code 3。codexは文字列判定のため過大計上の可能性）` の形へ展開する。1ソースのみの日は現行表記のまま(7/31 は claude-code 3 / codex 3 なので展開される)
- 内訳は `render_aiwork_markdown` が受け取っている `sessions` から `s.source` 別に集計する(stats を読み直さない)。`tools_measurable` でないセッション(ブラウザ)は現行どおりツール系集計から除外し、内訳にも出さない
- 既存の `codex_note`(aiwork.py:1207-1209)は展開形では括弧内に統合し、**同じ注記を二重に出さない**
- リトライ連鎖はセッションではなくプロンプト側の値(`retry_chain_count` 引数)なので、`retry_chains` 引数から source 別に数えられる場合のみ展開し、数えられない場合は合算のままにする(捏造禁止)
- 受け入れ条件: 2ソース混在セッション列で内訳が出て合算値と一致すること / 1ソースのみでは現行文字列と完全一致すること

### §A3 AI作業の質のカバレッジ宣言と注記抑止条件の修正(T2-3)

- `aiwork.py:1199` の直前(見出し 1197 と空行 1198 の直後、セッション行 1199-1202 の前)に**計測範囲の1〜2行**を追加する。1196 は `lines: list[str] = []` なので「1196 の直前」だとリスト生成の外へ出る。文面は列挙のみ(割り算禁止): `計測範囲: セッションログのある AI CLI / ブラウザ拡張のみが対象です。` + `画面計測のAI作業のうち chatgpt 71.7分・claude 34.8分 はログが無く、往復・エラー・トークンは計測できません。`
- 材料は `DailySummary.ai_tool_minutes`(`report.py:37` 定義・`report.py:220-230` で「AIツール別」表示に使用中。7/31 実績 `chatgpt 11m / claude 2m / openai 1m`)。`render_aiwork_markdown`(aiwork.py:1143-1157)に新しいキーワード引数 `screen_tool_minutes: Mapping[str, float] | None = None` を足し、`cli.py:238-250` の呼び出しで `summary.ai_tool_minutes` を渡す。**stats を読まない・新しい計測をしない**
- 「ログがある画面ツール」の判定は明示テーブルのみ: `{"chatgpt": ("chatgpt-web",), "claude": ("claude-web",), "gemini": ("gemini-web",)}`。当日のセッションに対応する `-web` ソースが1件も無いツールを「ログ無し」として列挙する。テーブルに無いツール名(`openai` / `copilot` / `cursor` / `perplexity` 等)は**ログ無し側に倒す**(fail-closed)
- **注意**: codex は端末上で動くため画面分類では「開発」になり `ai_tool_minutes` には決して現れない。`claude.exe`(デスクトップアプリ)は claude-code CLI とは別物なので、CLIセッションがあることを根拠に claude 画面時間を「計測済み」と書いてはいけない
- `advice_evidence.py:885` の条件式は**変えない**。885 の `if not (ai_stats_valid and telemetry_sessions > 0):` が出すのは 886-890 の既存注記「会話回数や回答品質は計測できない」であり、ここに OR を足すと CLIテレメトリのある日にその既存文が復活し、🧠 の往復数表示と矛盾する。代わりに 885-890 のブロックに **`elif` を足し**、「セッションログを持たないAI画面時間 >= 30分」のときだけ下記の新注記を `reader_notes` に足す。**両条件が重なる日は既存注記だけを出す**(既存注記が出る日は 🧠 自体が計測不能で、新文の「🧠の数値はCLI・拡張由来のみ」が成り立たないため)。判定材料として `stats.py:159-208` の stats 直下に `ai_screen_tool_minutes`(= `summary.ai_tool_minutes` を丸めた dict)を**追記のみ**で保存する。`build_stats` は既に `summary: DailySummary`(stats.py:41-44)を受け取っているので**新しい引数は足さず** `summary.ai_tool_minutes` から作る(二重ソース化して呼び出し漏れで注記が黙って消える経路を作らない)。**キー欠落の旧 stats は従来条件のまま**(fail-open せず、注記を増やさない側に倒す)
- 追加する注記文(達成断定・割り算なし): `セッションログを取得できないAI画面が{N}分記録されています。🧠の数値はCLI・拡張由来のみで、AI利用全体の質ではありません。`
- 受け入れ条件: `ai_screen_tool_minutes` に chatgpt 73.0 を持ち CLI セッションもある stats で `reader_notes` に当該注記が出ること / 同キーが無い旧 stats では出ないこと / 出力に `%` を含まないこと

## Phase 2 — §B: 🧠 AI作業の質

### §B1 本日の摩擦ワーストを日次1行で明示(T1-4)

- `aiwork.py:1122-1140 top_friction_sessions` を日次描画へ配線する。`render_aiwork_markdown` 内で `session_digests_for_stats(sessions, day_iso, redactor=redactor, retry_chains=retry_chains)`(aiwork.py:1086-1119)を呼び、`top_friction_sessions(digests, limit=1)` の1件を **`lines.append("")`(aiwork.py:1250) の直前**に挿入する(1249 は `if int(breaker_fires or 0) > 0:`(1248) の本文なので「1249 の直後」だと条件分岐の内側に入る)
- **注意: `session_digests_for_stats` の第2引数 `day: str`(aiwork.py:1088)は必須位置引数だが、`render_aiwork_markdown`(aiwork.py:1143-1157)は `day` を受け取っていない。** `day` は digest の `"day"` キー(aiwork.py:1103)にしか使われず、`top_friction_sessions` では同点時のソートキー(aiwork.py:1139)にしか効かない。したがって **新しい引数を増やさず**、`sessions[0].start.astimezone(tz).date().isoformat()`(セッション0件なら日次1行そのものを出さない)を渡す。ここに実日付以外の定数を入れて stats 側の digest と混同させないこと
- 出力形(3行。摩擦0の日は行ごと出さない): `⚠ 本日の摩擦ワースト: KaizenLog (codex)「今のローカルのコードベースに再度Readmeを作成して」` / `   — 摩擦454（ツールエラー444 ＋ 中断0×5 ＋ リトライ連鎖関与2×5）` / `   ※ 摩擦はスコア順位であり、AIの良し悪しの判定ではありません。`
- **書ける範囲の境界**: 実 stats で確認済みのとおり `session_digests` のキーは `day / session_id / project / title / tool_errors / interruptions / user_turns / edits / tests_run / ended_in_error / source / first_prompt_len / retry_touch / friction` のみ。**開始終了時刻もツール呼び出し総数も無い**。時刻やエラー率(444/717)を書きたくなっても、`digest` からは書かない。時刻を出すならレンダ側の `AISession` から引くこと(本弾では出さなくてよい)
- `title` は `session_digests_for_stats` 内で redact 済み。空文字なら `—` を出し、行そのものは残す。`_md_cell` を通して `|` を無害化する
- 表本体(aiwork.py:1261-1313)の並び順は**時刻順のまま**変えない(思い出しやすさ優先)。摩擦順に並べ替えないこと
- 受け入れ条件: friction 最大のセッションが表の3行目にあっても⚠行が表の**上**に出ること / 全セッション friction=0 の日は⚠行が出ないこと / `※ 摩擦はスコア順位` の注記が必ず同時に出ること

### §B2 コスト行とループ税行を意味へ変換(T2-4)

- `aiwork.py:1215-1227`: 換算不能側の分岐(`int(uncosted) > costed_tokens`)だけを差し替える。単価が登録されている日の `$X.XX` 表記は現行のまま。`推定コスト: 換算なし — 出力432,110 tok のうち単価未登録が431,521 tok。` / `未登録モデル: gpt-5.6-sol, gpt-5.6-terra, gpt-5.4, claude-opus-5, claude-fable-5。` / `kaizenlog.toml の [aiwork.pricing] に $/1Mtok を設定すると金額換算されます。` の3行
- 未登録モデル名は `sessions` の `s.models` から集め、`pricing` に無いものだけを列挙する。**桁区切り必須**。モデル名が0件なら該当行を省く(捏造禁止)
- `aiwork.py:995-1018 format_loop_tax_line`: 桁区切りと比較対象を足す。当日の `output_tokens` を新しいキーワード引数 `day_output_tokens: int | None = None` で受け取り、既知のときだけ割合を併記する。第34弾 §A2 の実計算側にある `total_wasted_tokens <= 関与セッションの output_tokens 一意合計` を壊さないこと。**100%超の表示は、意図的に不整合な入力を渡す防御的formatterテストに限定し、実データ再生成の期待値にはしない。**
- 最長エピソードの1行を足す。材料は `max_loop_episode`(aiwork.py:1021)の戻り値から取得し、**`LoopTaxEpisode` の往復数は `ep.chain.length`、浪費量は `ep.wasted_tokens`、エラー有無は `ep.has_tool_error` を使う。excerptは `retry_chain_excerpts` またはstats dictの既存値から取得し、存在しない属性を仮定しない。** `project` や `session_id` を持たないため、セッション帰属は書かない。
- `format_loop_tax_line` は `cli.py:263` の通知でも使われる。**引数を省略したときの出力が現行と1バイトも変わらないこと**を保証する
- 受け入れ条件: `day_output_tokens=None` で現行文字列と完全一致 / 重複チェーンfixtureで `total_wasted_tokens <= 関与セッションの一意output_tokens` を確認 / 不整合入力だけで `100%超` 注記が出ること / pricing 全登録の日に「換算なし」文が出ないこと

## Phase 3 — §C: 📊 Activity Log

### §C1 タイムラインの説明率と細切れ時間帯の明示(T1-2)

- `report.py:252-262`: `{min_label}以上の画面ブロックを時刻順に表示。` の直後に、**閾値未満**の説明を別行で足す — `表示外: 3分未満のブロック 277件・計136分（合計 3h39m の62%）。` / `内訳は AI作業 77分 / ブラウジング 25分 / コミュニケーション 15分 / エンタメ 10分。` / `細切れが集中した時間帯: 1時台 53件・34分、8時台 79件・23分、7時台 33件・21分。`
- 数値は `summary.blocks` から決定論で算出する(実 stats 2026-07-30 で 277件/136.1分/62%、**Asia/Tokyo 変換後**に 1時台53件34.4分・8時台79件22.8分・7時台33件21.3分 を検算済み)。内訳カテゴリは上位4件、時間帯は分の多い順に上位3件。時刻は `tz` へ変換してから時台を取ること(stats の `blocks[].start` は `+00:00` 保存であり、変換前だと 16/23/22時台という UTC の時台が出る)
- **`max_timeline_rows` 超過時の省略文(`report.py:256-261`)とは必ず別行にする**。「閾値未満で最初から対象外」と「対象内だが上限超過で省略」を1文に混ぜない。件数は実配列(`total_blocks` / `eligible_blocks` / `shown_blocks`)から取り、表に出した行数と一致させる
- `under_threshold_count > 0` ならeligible行が0件でも説明行を出す。閾値未満が0件なら追加行を出さない。閾値は固定値ではなく `min_block_minutes` から取り、割合は `合計に対する分の比`（件数比ではない）。
- 受け入れ条件: 7/30相当のblocksで説明行が出ること / 全件が閾値未満でも説明行だけが出ること / 上限超過も同時に起きるケースで2文が別行として両方出ること / 閾値未満0件で追加行が出ないこと

### §C2 前日からの変化テーブルと結論のトレンド文型化(T2-1)

- `report.py` に新関数 `render_change_table(today: Mapping, prev: Mapping | None) -> str` を足す。**両辺とも stats 形状の dict** を受け取り、片側でも欠ける指標の行は出さない(0埋め禁止)。行は `ツールエラー=ai.tool_errors` / `リトライ連鎖=ai.retry_chains` / `2往復以下のセッション=ai.fragmented` / `AI作業=by_category["AI作業"]` / `合計アクティブ=total_minutes` の5本。時間は `report.py:136 _fmt_minutes` を使う
- 末尾注記は**必須**: `※ 数値の向きだけを示します。稼働時間が短い日は多くの指標が自動的に下がるため、改善提案の効果を示すものではありません。` これが無いと ✅ の誤読を助長する。増減に良し悪しのラベル(改善/悪化)を付けない
- 配線: `cli.py:251-252` の `section = section.rstrip() + "\n\n" + aiwork_md` の**後**、`store.write_section(day, ACTIVITY_MARKER, section)`(cli.py:272)の**前**に `section` へ append する(272 より後に足すと、ノート本文にテーブルが無いのに `write_stats`(cli.py:296)の `activity_sha256` だけがテーブル込みで計算され、翌日の advise が `source_status="mismatch"` になる)。today は `stats.build_stats(day, summary, ai_sessions, input_stats, activity_md=None, retry_chains=..., pricing=..., title_redactor=..., loop_tax_summary=...)` で作り、prev は `load_stats(cfg.stats_path, days=2, end_day=day)` から `day-1` の1件だけを取る。**当日の既存 stats ファイルは読まない**(前回実行の残骸を自分と比較する事故を防ぐ)
- `write_stats` は `activity_md=section` から `activity_sha256` を計算するので、**テーブルを append した後の section を渡す**こと。順序を逆にすると advise 側が `source_status="mismatch"` になり stats が丸ごと捨てられる
- 前日 stats が無い日はテーブルごと出さない
- `advice_evidence.py:327-416 _build_reader_summary` の結論をトレンド文型へ寄せる。`history` は既に引数で受け取っている。3文の枠と「記録されています」の記録調は維持し、以下だけ変える:
  - 第1文: 履歴3日以上あり `by_category["AI作業"]` が**暦日として隣接する記録日に**単調増加している場合のみ `AI作業は{N}日連続で増加し、本日 1時間54分が記録されています。` を出す。欠損日がある場合は「記録のあるN日で単調増加」とし、条件を満たさなければ現行の合計文へ戻す。
  - 集中文(`_tool_error_concentration_sentence`, advice_evidence.py:264)に**割合を併記**する(`639回のうち538回（84%）` — 分母分子とも同一 stats 由来なので割り算可)
  - 直上 Activity Log の逐語再掲になっている「カテゴリ別では『X』が最多」(advice_evidence.py:361-370)は、履歴2日以上**かつ当日の `total_minutes` が当日を含む記録日の最大値と一致するときだけ** `合計 3時間39分 は記録のある直近{N}日で最長です。` に置き換える。最長でない日と履歴2日未満は現行テンプレへフォールバックし、順位や「N番目」を**捏造しない**(実 total_minutes は 7/26=101.8 / 7/27=194.3 / 7/28=189.3 / 7/29=135.8 / 7/30=219.5 / 7/31=64.6 で、無条件に出すと 7/31 が虚偽になる。本弾の追加制約(b)達成断定禁止と整合させる)
- 受け入れ条件: 履歴0日で結論が現行と完全一致 / 5日増加履歴で「5日連続で増加」が出ること / 変化テーブルに必ず稼働短縮の注記が付くこと / 前日 stats 欠落でテーブルが出ないこと

## Phase 4 — §D: 🚀 Kaizen

### §D1 LLM解釈の復活(なぜ / 明日見る数字 / AI作業の見立て)(T2-2)

- `advisor.py:570-589 render_reader_advice` は現在 `明日の最小アクション` のチェックボックス行だけを拾い、`計画と実績` / `今日の改善提案` / `AI作業の改善` を丸ごと捨てている。`_level3_sections`(advisor.py:518-527)は既に全見出しを返しているので、材料は揃っている
- `### 明日試すこと` の各チェックボックス行の下に、**インデント付きの素の箇条書き2行**を足す(`    - なぜ: 同じ題材の参照画面を開いたまま依頼を始めた日に、言い直しの連鎖が観測されています（解釈であり因果の断定ではありません）` / `    - 明日見る数字: リトライ連鎖`)。素材は `今日の改善提案` の `i` 番目の行(`{i}. {interpretation}。{proposal}。翌日見る指標: {next_metric}` — advice_format.py:582-588)。`proposals` と `actions` は契約上**同数・同順**(prompts/daily_advisor.md「件数と対応」)なので index で対応付ける。対応行が取れないアクションはサブ行を付けない(捏造禁止)
- `### 明日試すこと` の後ろに `### AI作業の見立て` を足し、`AI作業の改善` の各 `- ` 行をそのまま並べる。空なら見出しごと出さない
- **ID衝突の予防線(必須)**: サブ行を絶対に `- [ ]` 形式で書かないこと。`memory.py:22 _CHECKBOX_RE = ^(\s*- \[)([ xX\-])(\]\s*)(.*)$` は**インデント付きチェックボックスにもマッチする**ため、`assign_action_ids`(memory.py:124)がサブ行にも KZN-ID を採番し `suggestions.jsonl` を汚染する。サブ行は `    - なぜ: ` のような素の箇条書きに限定する
- **分業を崩さない**: F-ID 除去(`advisor.py:576 _FACT_ID_RE`)は維持する。`interpretation` は `advice_format.py:396-399` で算用数字が禁止されているため、**数字は決定論側(PASS条件・next_metric)にだけ現れる**。サブ行に数字を後付けしない
- **保存形式を変えない**: `- [ ]` 行本体の `｜PASS: … ｜FAIL: …` 構文は1文字も変えない。`verdict.parse_pass_condition` の入力は `memory` の action 文字列(= このチェックボックス行のテキスト)なので、ここを崩すと全KZNが判定不能になる
- `render_reader_advice` は `cli.py:1016` から呼ばれ、その後段 `cli.py:1018 assign_action_ids` → `cli.py:1028 _write_actions_handoff`(定義 cli.py:517-531)が走る。転記されるのは `MemoryEntry.action`(チェックボックス行本文)のみで、サブ行は翌日ノートへ持ち越されない — これは期待動作
- 受け入れ条件: サブ行を含む出力を `assign_action_ids` に通して**新規エントリ数がアクション数と一致**すること(サブ行にIDが振られないこと) / 出力に `[F1]` 等が残らないこと / `parse_pass_condition` が各行から従来どおり指標を取れること / `ai_review` 空で `### AI作業の見立て` が出ないこと

### §D2 非挑戦的PASSの閾値ゲートを履歴中央値基準へ(T1-5)

- `advice_evidence.py:237-261 _metric_baselines_from_stats(stats)` を `_metric_baselines_from_history(stats, history)` に置き換える(呼び出しは advice_evidence.py:940 の1箇所)。各指標について `history` から `stats["day"]` と同じ日を除いた値を集め、**暦日と値の両方が有効な3日以上だけ中央値**を入れる。欠損mapping・欠損キー・測定不能値は0に変換せず除外し、3日未満の指標はキーを入れない(= 既存どおり検査スキップ)
- 対象は `_BASELINE_SIMPLE_METRICS`(advice_evidence.py:220-234)と `category_minutes:*` / `site_minutes:*`。値の取り出しは既存の `experiments.metric_from_stats` を使う
- **やってはいけないこと(最重要)**: 「当日実測と中央値の**厳しい方(min)**」を取ってはいけない。7/31 は `context_switches=127`(実 stats 2026-07-31)に対し当日を除く 7/26-7/30 の中央値が210で、min を取ると低稼働日にどの閾値も通らず、HANDOFF.md「ガード強化でLLMが検証の緩い経路に流れる(自由文PASS事件)」の再発になる。**当日実測へのフォールバックも禁止**(履歴不足はスキップ)
- `advice_format.py:38-39` に**別定数**を足す: `_MEDIAN_CHALLENGE_LE = 0.95` / `_MEDIAN_CHALLENGE_GE = 1.05`(最小意味差分)。`_pass_challenge_error`(advice_format.py:43-92)の係数をこの2つへ差し替える。既存の `_CHALLENGE_LOOSE_LE/GE`(1.2/0.8)は差し替え後に参照ゼロになるので**未使用定数として削除する**(現在の参照は advice_format.py:79/83/86/90 の `_pass_challenge_error` 内だけで、F10帯は advice_evidence.py:808/829/840/849 が 0.85/0.95 をハードコードしており両定数を使っていない。削除前に他モジュール・テストからの参照が無いことを grep で確認する)。エラー文には `緩すぎ` の語を残す(既存テストの判定語)
- `advisor.py:703-732 _baseline_repair_hint` の見出し `## ベースライン（当日実測・PASS はこの値より挑戦的に）` を `## ベースライン（直近履歴の中央値・PASS はこの値より挑戦的に）` へ直す。**中身が中央値なのに「当日実測」と表示し続けるのは fail-closed 原則違反**
- **既存テストの更新が必要**: `tests/test_round16_journal_value.py:51-64 test_n1_ge_challenge_bounds` は baseline 3.4 に対し `focus_blocks >= 2.8` を合格としているが、新係数では `3.4×1.05=3.57` 未満のため不合格になる。期待値を `>= 3.6` 側へ更新すること(仕様変更であり、テストを緩めるのではない)。`test_n1_loose_le_rejected_tight_accepted` と `test_n1_no_baseline_skips_challenge` は無変更で通るはず
- 受け入れ条件: 履歴2日のみの指標でベースラインが作られず検査がスキップされること / 履歴5日 `ai_tool_errors=[101,770,121,178,639]` で中央値178が入り `<= 200` が拒否・`<= 169` が合格すること / `metric_baselines` に当日実測値が1件も混ざっていないこと

## Phase 5 — §E1 テスト・ドキュメント・実挙動確認

- `tests/test_round35_journal_information_design.py` を新規作成し、上記の**受け入れ条件を §ID ごとに1テスト以上**で固定する。既存の弾別テストファイルには足さない
- 片側だけの文面検証にしないこと(HANDOFF「テストが通るのに実挙動が違う」): §A2・§B2・§C1 は**出す日と出さない日の両方**、§A1・§D1 は**空入力と非空入力の両方**を必ず検証する
- `tests/test_round16_journal_value.py` の §D2 該当ケースを更新(上記)。他ファイルの既存アサーションを緩める変更は禁止
- `docs/USAGE.md` に「計測範囲(§A3)」「ループ税の100%超の意味(§B2)」「PASS閾値ベースラインが履歴中央値になったこと(§D2)」を追記

## 仕上げ

1. `./.venv/Scripts/python.exe -m pytest -q` 全PASS（開始時に取得した基準線 + 本弾の追加分。固定値705を実行結果の代わりに使わない）
2. **実挙動確認(必須)**: まず一時fixtureまたは承認済みコピーで7/30・7/31相当の2日分を確認し、9件すべての表示を確認する。Activity/Advice/Actionsのマーカー外に加え、stats、`suggestions.jsonl`、実験ファイル、run log のSHA/行数を前後比較し、許可外副作用が無いことを確認する。実ボールトを使う場合はユーザー承認・バックアップ・復元条件を先に満たす。
   **リポジトリ直下の `kaizenlog.toml` を `--config` に直接指定しない**(`kaizenlog.toml:7` の `vault_dir='C:/develop/obsidian/2026'` により実日誌と実 stats を上書きし、7/30 を再生成すると §C1/§B2/§D2 の根拠 stats が失われる)。一時ディレクトリへコピーし `[general] vault_dir` を一時パスへ書き換えた toml を使う。
   ```
   ./.venv/Scripts/python.exe -m kaizenlog.cli --config <TMP>/kaizenlog.toml generate --date 2026-07-30
   ./.venv/Scripts/python.exe -m kaizenlog.cli --config <TMP>/kaizenlog.toml generate --date 2026-07-31
   ./.venv/Scripts/python.exe -m kaizenlog.cli --config <TMP>/kaizenlog.toml advise --date 2026-07-30 --dry-run
   ```
   (`advise` の実LLM呼び出しは禁止。`--dry-run` で送信内容とベースライン表示だけを確認し、Kaizen区間の描画は `render_reader_advice` を単体で呼ぶ python スクリプトで確認する)
3. 完了報告は §ID 判定表(✅/⚠️/❌ + file:line + 追加テスト名)。未完は正直に。テスト未通過で ✅ 禁止

| §ID | 判定 | 根拠 (file:line) | 追加テスト |
|---|---|---|---|
| §A1 | | | |
| §A2 | | | |
| §A3 | | | |
| §B1 | | | |
| §B2 | | | |
| §C1 | | | |
| §C2 | | | |
| §D1 | | | |
| §D2 | | | |
| §E1 | | | |
