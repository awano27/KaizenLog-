# 第50弾: 第49弾レビュー残件修正 指示プロンプト

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`（判定は HEAD + ワーキングツリー。第49弾 = コミット `3c0ef2a`）

この指示書は**実装指示書として読み、この指示書自体は編集しない**こと。

## 0. 前提・注意

- 第49弾の実挙動レビュー + 変異テストで検出した残件の修正。実装の骨格は正しいので、**指定箇所以外は触らない**。
- **着手前に `git status` を確認**: ワーキングツリーに並行セッション（PLAN.md 記載の Graph Engineering 作業）の未コミット変更がある（`advice_format.py`・`digest.py`・`prompts/*.md`・テスト7ファイル・`tests/test_graph_engineering_journal_advice.py` 等）。**これらの並行変更ハンクを上書き・破棄しない**。自分の修正対象ハンクだけを変更する。

## P0（プライバシー不変条件の破り — リリース前必須）

- **R1 (C1残)**: `cli.py:1899` `_patch_stats_goal_achieved` が `goal_stats_fields(day_goal, None)`（redactor なし）で stats の `goal_text` を上書きし、generate が redact 済みで保存した値を**生テキストに戻す**。実証済み: `[REDACTED] の提案書` → `ACME-SECRET の提案書` になり、`[F14]` 経由で生値が LLM プロンプトへ流れる（goal.py:275-279 / advice_evidence.py:747 の不変条件違反）。
  修正: `goal_achieved` キー**のみ**をパッチし `goal_text` / `goal_category` に触れない（または cli.py:272 と同一の `make_redactor(cfg.privacy...)` を通す）。
  受け入れ: redact 済み stats に対し `goal --achieved N` 実行後も `goal_text` が redact 済みのままで、`build_advice_evidence` の [F14] に生値が出ない。
- **R2 (D2残)**: 依頼側 digest が「60字切詰め → redact」の順（`aiwork.py:1327` で切詰め済みの `prompts_digest` を `aiwork.py:1890-1891` で redact）。境界跨ぎトークンがノートと stats の両方に露出することを実証済み（`- 依頼: 「aaa...SECRET_T」`）。D1 と同じ **redact → 切詰め**の順に修正。あわせて仕様の**80字**に統一（`aiwork.py:1896` と `advice_evidence.py:1288` の `[:80]` は入力が60字済みのため死んでいる — 生かすこと）。
- **R3 (D4残)**: `advice_evidence.py:761` の `[F14b]` は fact-ID 正規表現 `\[F\d+\]`（advice_evidence.py:77, advisor.py:529）に不一致。citable な fact_ids に入らず（実測: F14b 欠落）、モデルが引用した場合 `_FACT_ID_RE.sub`（advisor.py:679/712）で除去されず生 `[F14b]` が描画に漏れる。
  修正: 数値 ID **[F21]** に変更（F20 = advice_evidence.py:1298、F19 = advice_evidence.py:1159、F18 = coachledger.py:417 は使用済み。変更後に全 src/ で衝突 grep すること）。

## P1（挙動の正しさ）

- **R4 (C2残)**: `cli.py:991-995` で stats の `goal_achieved` がノートの値より優先されるため、`goal "A"` → `--achieved 80` → `goal "B"` の後に digest が「目標B ｜ 達成度: 80%（自己申告）」と**旧目標の達成度を新目標に帰属**させる（実証済み）。ノートの GOAL 区間を正とし、stats は「ノートに GOAL 区間が無い場合の fallback」のみに変更（ノートに達成度行がなければ未申告扱い）。
- **R5 (D残)**: `aiwork_codex.py:408` の `event_msg`/`agent_message` 分岐がアシスタント本文を捨て `last_reply_digest=None` になる（実証済み: response_item 形式のみ捕捉）。この分岐でも text を拾い、`response_item` と同じ redact→120字経路に乗せる。
- **R6 (C2残)**: 目標カテゴリ実測分数のルックアップが3重複（`advice_evidence.py:441-451` / `digest.py:191-196` / `nippou.py:469-476`）。1つの共有ヘルパーに集約（配置は goal.py か stats.py。digest.py は並行変更中のため、該当ハンクの置換のみ最小差分で）。

## テスト補強（変異テストで生存した穴 — 既存 `tests/test_round49_goal_achievement_ai_io.py` に追記。残件弾の慣例どおり新ファイルは作らない）

- **T1**: `cmd_generate` を実際に通す統合テスト。次の2変異が**落ちる**こと: (a) `cli.py:862` の `_resync_stats_activity_fingerprint` 呼び出し削除（現状全テスト生存を確認済み）、(b) `cli.py:597` 相当の `goal_achieved=None` 化（同じく生存確認済み）。
- **T2**: `main(["run", ...])` を通す統合テスト。`cli.py:4324-4335` の auto-nippou gate 無効化変異が落ちること（既定 config で NIPPOU が書かれる / `auto_write=false` で書かれない / `advise` 単独では書かれない）。
- **T3**: D1 テストの秘密トークンを**120字境界を跨ぐ位置**に変更（現状は先頭配置のため truncate-first 変異が生存 — 確認済み）。R2 側にも80字境界跨ぎの同型テストを追加。
- **T4**: D2 の `count("- 依頼:") <= 5` を両側断言に（7セッション入力で `== 5`）。依頼80字断言・codex `last_reply_digest`（event_msg 形式含む）・[F20]/[F21] が `fact_ids` に入る断言を追加。
- **T5**: 素の「※」だけの行を含む本文で `consolidate_disclaimers` が冪等であること（現状 `vault.py:202` の空定義 reject で2回目に脚注セクション消滅 — 確認済み。空 ※ をスキップする等で冪等化）。

## 対象外（記録のみ・今回修正しない）

- generate 内 `source_status="verified"` ハードコード（cli.py:836-840）: 第49弾以前からの既存挙動で実害なし。
- ACTIVITY より前のセクションが resync 後に ※ を増やすと fp が再び割れる潜在問題（現行経路では到達不能）: 次弾以降の設計課題。
- `test_round31_guard.py::test_c1_import_lightness` の失敗は環境要因（subprocess が PYTHONPATH を継がない）で第49弾と無関係。

## 禁止事項（恒常）

- **git commit / push 禁止**（第49弾で違反が発生した。今回は厳守。変更はワーキングツリーに残すだけ）
- 実 LLM 呼び出し禁止・実ボールト `C:\develop\obsidian` への書き込み禁止（テストは tmp_path）
- 並行セッションの未コミット変更（advice_format.py ほか上記）の上書き・revert 禁止
- ssh / scp / リモートアクセス禁止・stats JSON の破壊的変更禁止

## 受け入れ・完了報告

- `pytest -q` で全 green（`test_c1_import_lightness` の環境失敗のみ許容）。passed 数を明記。
- R1〜R6 各修正は該当実装行の無効化で対応テストが落ちること（変異確認の実施結果を報告に含める）。
- 項目ID（R1〜R6, T1〜T5）ごとに 実装済み/未実装 + 主要変更点1行 + 変更ファイル一覧。最終判定はレビュー側が HEAD + ワーキングツリーで再検証する。
