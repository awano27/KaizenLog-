# Codex 実装指示: 運用衛生4件(doctor拡充・リポジトリ清掃)— 第22弾

対象リポジトリ: `C:\develop\KaizenLog\KaizenLog-`(Python / pytest)

**前提: 第21弾適用済みのワーキングツリー。** 4件とも小粒・独立。

## 背景(実査からの証拠)

- スケジュールタスク(夜間21:30 / 朝08:30)が**いまだに未登録**で、2026-07-29 朝の morning が走っていない。doctor は「すべて正常です」と報告する — **最も痛い故障(タスク消失=データ欠測)を診断できない**。検出関数 `setup_detect.is_task_registered`(setup_detect.py:125)は実装済みなのに setup からしか呼ばれていない
- doctor は第10〜21弾で入った成果物(Kaizen Memory / experiments / Weekly Reviews / browser_export_dir)を一切診断しない(doctor.py の import は config と runlog のみ)
- `git status` が毎回 Permission denied 警告10行(.codex-pytest-tmp/)+未追跡70件で汚れており、本当の変更が見えない

## §H1 [High] doctor にスケジュールタスク登録チェック

`src/kaizenlog/doctor.py`、`src/kaizenlog/setup_detect.py`(再利用)

1. `_check_schedule`: `is_task_registered` で夜間タスク(KaizenLog)と朝タスク(KaizenLog Morning)の登録を確認。**夜間未登録は error**(「夜間タスクが未登録です。毎晩の記録が自動で走りません: `kaizenlog setup --register-task`」)、朝未登録は warn。schtasks 呼び出し失敗(非Windows等)は info でスキップ
2. 既存チェック一覧の様式・実行順に合わせる(接続系の後)

テスト: 登録あり/なし/検出失敗の3分岐(subprocess はモック)。

## §H2 [Medium] doctor に成果物チェック(_check_artifacts)

`src/kaizenlog/doctor.py`

1. 以下の存在+概況を1チェックにまとめて報告:
   - `cfg.memory_path`(suggestions.jsonl): 存在時はエントリ件数、パース不能行があれば warn
   - `cfg.experiments_path`: ディレクトリ存在とノート件数
   - `<daily_notes>/Weekly Reviews/`: 直近ノートの有無(無くても info 止まり — weekly は任意機能)
   - `[aiwork] browser_export_dir`: 設定が有効でディレクトリが存在しない場合のみ warn(拡張未導入は正常状態なので error にしない。この理由をコメントに明記)
2. いずれも「無い=即異常」ではないものは info/warn に留め、doctor の全体判定を壊さない

テスト: 各成果物の有無パターン / 破損JSONL行の warn。

## §H3 [High] .gitignore 追加2行

`.gitignore`

1. `.codex-pytest-tmp/` と `grok-desktop-experiment/` を追加(前者はACL問題で毎回警告10行、後者は別プロジェクト42ファイル)

## §H4 [Medium] Codex指示書mdの整理

ルート直下の `0725_*〜0729_*_Codex_*指示プロンプト_第N弾.md` 28本

1. `docs/codex-prompts/` を作成して全て移動(ファイル名は変更しない。mv のみ — **git 操作はしない**、追跡するかはユーザーがコミット時に決める)
2. `docs/codex-prompts/README.md` を新規作成: 「この改善ループの指示書アーカイブ。第N弾の番号順に適用された。運用ルール(コミット禁止・レビュー駆動)の要約」を10行程度で

テスト: 不要(ファイル移動のみ)。pytest が引き続き全件通過することのみ確認。

---

## 受け入れ条件

- `python -m pytest -q` 全件通過(既存デグレなし)
- 修正後に `kaizenlog doctor` を実行し、タスク未登録の error が実際に表示されることを確認して報告に含める(実環境が未登録である今がテストチャンス)

作業完了時: 変更ファイル一覧、§H1〜H4 の対応状況、doctor 実行結果の要約、pytest 結果(要約)を報告すること。

## 禁止事項(毎回共通・厳守)

- **git commit / push / branch 操作を絶対にしない(変更・移動はワーキングツリーに残すだけ)**
- ssh / scp / リモートアクセスをしない
- DB スキーマ変更をしない
- テスト・実装から実 LLM・ネットワークを呼び出さない
- 外部ライブラリを追加しない(標準ライブラリのみ)
- **タスクスケジューラへの登録・削除をテスト・実装から実行しない(検出のみ。登録はユーザーが行う)**
- バージョン番号の変更・CHANGELOG のリリース節の追加をしない
- マーカー区間外のノート内容・手書きテキストを変更するコードを書かない
